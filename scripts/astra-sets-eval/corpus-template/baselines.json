{
  "_template": true,
  "_readme": "Optional: an operator's read-only export from production. The runner never reads production itself. Without it, C compares against its own control arm, and nonzero output-gate refusals are UNDETERMINED.",
  "identity": [
    {
      "characterId": "char-a",
      "engine": "gpt-image",
      "scores": [],
      "source": "<<FORMAT ONLY — the query or screen these match_score values came from>>",
      "readOn": "0000-00-00"
    }
  ],
  "outputGateStrictLane": {
    "renders": 0,
    "refusals": 0,
    "window": "<<FORMAT ONLY — the dates covered>>",
    "source": "<<FORMAT ONLY — the query these counts came from>>",
    "readOn": "0000-00-00"
  }
}
