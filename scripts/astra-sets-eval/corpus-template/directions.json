[
  "<<FORMAT ONLY — NOT A DIRECTION: one action a person does in a frame, fitting any location, at most 300 characters>>",
  "<<FORMAT ONLY — NOT A DIRECTION: a second action (the real file needs at least three)>>"
]
