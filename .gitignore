# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (secrets — never commit these)
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Scratch folder for files this environment can't unlink (see session notes).
_to_delete/

# Local snapshot/fix-bundle archives (never commit these)
_*.tgz

# Standalone read-only admin console (separate app, deployed on its own)
picacho-admin/
picacho-admin.zip
